<%= form_for(@award) do |f| %>
  <% if @award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@award.errors.count, "error") %> prohibited this award from being saved:</h2>

      <ul>
      <% @award.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.collection_select :person_id, Person.where(active: true).order(:surname,:forename), :id, :display_name %>
  </div>
  <div class="field">
    <%= f.label :rank_id %><br>
    <%= f.collection_select :rank_id, Rank.where(active: true).order(:style_id,:order), :id, :style_and_rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
